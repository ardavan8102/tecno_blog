import 'package:flutter/material.dart';

class RegistrationCompeletePage extends StatelessWidget {
  const RegistrationCompeletePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}